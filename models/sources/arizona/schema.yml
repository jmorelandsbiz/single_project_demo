
version: 2
groups:
  - name: az_zip_codes
    owner:
      name: Arizona Zip Code Team
      email: zips@phdata.io

models:
    - name: az_zip
      
      access: public
    - name: pre_az_zip
      
      access: protected
      config:
        contract:
          enforced: true
      description: "Arizona Zip Codes"
      columns:
        - name: zip_id
          data_type: number(19,0)
          constraints:
            - type: not_null
            - type: unique
              warn_unenforced: false
          tests:
            - unique
        - name: zip
          data_type: varchar
          constraints:
            - type: not_null
            - type: unique
              warn_unenforced: false
          tests:
            - unique
        - name: sourcesystem
          data_type: varchar
        - name: city
          data_type: varchar
        - name: state
          data_type: varchar
